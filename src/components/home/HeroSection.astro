---
import Button from '../shared/Button.astro';
---

<section class="hero">
  <div class="container hero-content">
    <h1 class="hero-title">Montessori for Adolescents</h1>
    <p class="hero-subtitle">
      Helping communities build Montessori-inspired adolescent programs
      from the ground up — especially those who've never had access.
    </p>
    <div class="hero-actions">
      <Button href="/support/" variant="primary" size="lg">Our Work</Button>
      <Button href="/access/" variant="outline" size="lg">Support Access</Button>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding-block: var(--space-5xl);
    text-align: center;
    overflow: hidden;
    min-height: 60vh;
    display: flex;
    align-items: center;
    background: linear-gradient(135deg, var(--color-purple) 0%, var(--color-earth-dark) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-title {
    font-size: var(--text-5xl);
    color: var(--color-cream);
    margin-bottom: var(--space-lg);
    letter-spacing: var(--tracking-tight);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: rgba(250, 247, 242, 0.85);
    max-width: 620px;
    margin-inline: auto;
    margin-bottom: var(--space-2xl);
    line-height: var(--leading-relaxed);
  }

  .hero-actions {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  .hero-actions :global(.btn--outline) {
    color: var(--color-lavender);
    border-color: var(--color-lavender);
  }

  .hero-actions :global(.btn--outline:hover) {
    background-color: var(--color-lavender);
    border-color: var(--color-lavender);
    color: var(--color-purple);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 40vh;
      padding-block: var(--space-3xl);
    }
  }
</style>
