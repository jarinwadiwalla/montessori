---
import PageLayout from '../layouts/PageLayout.astro';
import BuyMeACoffee from '../components/shared/BuyMeACoffee.astro';
import QuoteBanner from '../components/shared/QuoteBanner.astro';
---

<PageLayout
  title="Access"
  description="Bringing Montessori adolescent programming to communities that have never had access — equity is not a value statement, it's our core mission."
  heroTitle="Montessori for Everyone"
  heroSubtitle=""
>
  <div class="container container--narrow">
    <section class="page-section">
      <p>
        Montessori training and schooling remain out of reach for many. Diploma courses can cost
        upward of €13,000, and the majority of Montessori programs worldwide are private
        institutions with tuition that most families simply can't afford.
      </p>
      <p>
        We raise funds and build partnerships to bring Montessori adolescent programming
        to communities that haven't had access.
      </p>
    </section>

    <section class="page-section">
      <h2>How Your Support Helps</h2>
      <div class="impact-grid">
        <div class="impact-card">
          <h3>Training</h3>
          <p>Funding Montessori adolescent training for educators in communities where the cost would otherwise be a barrier.</p>
        </div>
        <div class="impact-card">
          <h3>Programs</h3>
          <p>Helping communities create their own spaces for adolescents to learn, work, and grow together.</p>
        </div>
        <div class="impact-card">
          <h3>On-Site Support</h3>
          <p>Being there in person — side by side — to help turn a vision into something real.</p>
        </div>
      </div>
    </section>

    <section class="page-section text-center">
      <h2>Support Access</h2>
      <p>
        Every contribution helps bring Montessori wisdom to communities that have never had the opportunity.
        This is how we build a more equitable world — one adolescent program at a time.
      </p>
      <div class="donate-action">
        <BuyMeACoffee label="Support Our Work" />
      </div>
    </section>
  </div>

  <QuoteBanner
    text="Education should no longer be mostly imparting of knowledge, but must take a new path, seeking the release of human potentialities."
    author="Maria Montessori"
    source="The Absorbent Mind"
  />
</PageLayout>

<style>
  .page-section {
    margin-bottom: var(--space-3xl);
  }

  .page-section h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-lg);
    color: var(--color-earth);
  }

  .page-section p {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text);
    margin-bottom: var(--space-md);
  }

  .impact-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .impact-card {
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--space-xl);
  }

  .impact-card h3 {
    font-size: var(--text-xl);
    color: var(--color-terracotta);
    margin-bottom: var(--space-sm);
  }

  .impact-card p {
    font-size: var(--text-sm);
    margin-bottom: 0;
  }

  .donate-action {
    margin-top: var(--space-lg);
    display: flex;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .impact-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
